<Window
    x:Class="Doxie.AddExtensionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Doxie"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Add inclusion or exclusion"
    Width="350"
    Height="250"
    MinWidth="350"
    MinHeight="250"
    FocusManager.FocusedElement="{Binding ElementName=definition}"
    FontFamily="Consolas"
    FontSize="12"
    ResizeMode="CanResizeWithGrip"
    WindowStartupLocation="CenterOwner"
    WindowStyle="ToolWindow"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Margin="10"
            VerticalAlignment="Center"
            Text="Definition" />

        <TextBox
            x:Name="definition"
            Grid.Column="1"
            Grid.ColumnSpan="3"
            Margin="10,10,15,10"
            MaxLines="1"
            Text="{Binding Inclusion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel
            Grid.Row="1"
            Grid.ColumnSpan="4"
            Orientation="Horizontal">
            <CheckBox
                Margin="10"
                VerticalAlignment="Center"
                Content="Is regular expression"
                IsChecked="{Binding IsRegex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <CheckBox
                Margin="10"
                VerticalAlignment="Center"
                Content="Is exclusion"
                IsChecked="{Binding IsExclusion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <TextBlock
            x:Name="help"
            Grid.Row="2"
            Grid.ColumnSpan="4"
            Margin="10,10,15,10"
            Text="{Binding Description}"
            TextTrimming="CharacterEllipsis" />

        <Button
            x:Name="ok"
            Grid.Row="3"
            Grid.Column="2"
            Width="70"
            Margin="5,5,5,10"
            Click="Ok_Click"
            IsDefault="True">
            OK
        </Button>
        <Button
            x:Name="cancel"
            Grid.Row="3"
            Grid.Column="3"
            Width="70"
            Margin="5,5,15,10"
            Click="Cancel_Click">
            Cancel
        </Button>
    </Grid>
</Window>
